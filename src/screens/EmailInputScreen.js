import React from 'react';
import {View, Text} from 'react-native';

const EmailInputScreen = ()=>{
    return(
        <View>
            <Text>Email Input Screen</Text>
        </View>
    )
};

export default EmailInputScreen;